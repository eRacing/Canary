package com.polymtl.eracing.cananalyzer.model.signal;

/**
 * This enumeration represents a signedness of a signal.
 */
public enum Signedness {
    /**
     * A signal that can contain both positive and negative value.
     */
    SIGNED,

    /**
     * A signal that can contain only positive value.
     */
    UNSIGNED;
}
